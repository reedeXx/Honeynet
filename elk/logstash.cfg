input {
	file {
	  path => "/home/ubuntu/temp/inlog.log"
	}	
}

output {
	elasticsearch {
	  hosts => ["http://elasticsearch:9200"]
          codec => line { format => "custom format: %{message}" }	
	}
	
}
